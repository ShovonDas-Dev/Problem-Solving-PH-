// Below are three small broken snippets. For each, identify the error type (Syntax / Runtime / Logic), then fix it.
// Input
// Expected Output
// sayHi("Mira")
// Hi, Mira


function sayHi(name) {
//   console.log("Hi, " + name)  // find the bug
}
 
sayHi("Mira");

// Resolved



// Snippet B
let word = "hello";
let words = word.toUpperCase()
console.log(words); // find the bug
// Resolved


// Snippet C
function square(num) {
  return num * num; // find the bug
}
 
console.log(square(4)); // should be 16
// Resolved

